# CI/CD (зарезервировано)

Папка зарезервирована под GitHub Actions workflows.

Активная CI-конфигурация (lint + build + test на каждый PR) добавляется на Этапе 12
("Безопасность и производительность") / 13 ("Тестирование") Roadmap, когда в проекте
появится достаточно тестов, чтобы pipeline имел смысл.
